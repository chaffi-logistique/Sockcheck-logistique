kind = "web"
previewPath = "/"
title = "StockCheck"
version = "1.0.0"
id = "artifacts/stockcheck"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18991

[services.development]
run = "pnpm --filter @workspace/stockcheck run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/stockcheck", "run", "build" ]
publicDir = "artifacts/stockcheck/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18991"
BASE_PATH = "/"
